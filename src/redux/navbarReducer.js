let initialState = {
    friends:
        [
            {id: 1, name: "Sasha"},
            {id: 2, name: "Andrei"},
            {id: 3, name: "Oleg"}
        ]
}

const navbarReducer = (state = initialState,action) => {

    return state
}

export default navbarReducer